name: kokoro-v1
version: 1.0.0
engine: kokoro
type: tts
description: "Kokoro TTS v1"

capabilities:
  streaming: false
  languages: ["en", "pt"]

resources:
  memory_mb: 1024
  gpu_required: false
  gpu_recommended: true
  load_time_seconds: 5

engine_config:
  device: "auto"
